{
	"name": "bound",
	"private": true,
	"workspaces": ["packages/*"],
	"scripts": {
		"test": "bun test --recursive",
		"test:server": "bun run ./scripts/start-test-server.ts",
		"test:e2e": "bunx playwright test e2e/",
		"lint": "bunx @biomejs/biome check .",
		"lint:fix": "bunx @biomejs/biome check --write .",
		"typecheck": "tsc -p packages/shared --noEmit && tsc -p packages/core --noEmit && tsc -p packages/sync --noEmit && tsc -p packages/sandbox --noEmit && tsc -p packages/llm --noEmit && tsc -p packages/agent --noEmit && tsc -p packages/discord --noEmit && tsc -p packages/web --noEmit && tsc -p packages/cli --noEmit",
		"build": "bun run scripts/build.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/bun": "latest",
		"typescript": "^5.7.0"
	}
}
